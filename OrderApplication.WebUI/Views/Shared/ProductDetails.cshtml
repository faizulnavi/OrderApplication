@model OrderApplication.Domain.Entities.Product

<div class="well">
    <h3>
        <strong> @Model.Product_Name </strong>
        <span class="pull-right label label-primary">@Model.Product_Price.ToString("c") </span>
    </h3>
    @*@using (Html.BeginForm("AddToCart", "Cart"))
    {
        <div class="pull-right">
            @Html.HiddenFor(x => x.Product_ID)
            @Html.Hidden("returnUrl", Request.Url.PathAndQuery)
            <input type="submit" class="btn btn-success" value="Place Your Order" />
        </div>
    }*@

    @using (Html.BeginForm("AddToCart", "Cart"))
    {
        <div class="pull-right">
            @Html.HiddenFor(x => x.Product_ID)
            @Html.Hidden("returnUrl", Request.Url.PathAndQuery)
            <input type="submit" class="btn btn-success" value="Add to cart" />
        </div>
    }
    @*<span class="lead">@Model.Description </span>*@

</div>
